import { colors } from '@/constants/Colors';
import Ionicons from '@expo/vector-icons/Ionicons';

export default function SettingsButton() {
    return <Ionicons name="ellipsis-horizontal" size={40} color={colors.primaryText} />
}